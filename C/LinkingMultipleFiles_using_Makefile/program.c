#include <stdio.h>
#include <stdlib.h>
#include "func_prototypes.h"

int main(void){
    printf("whaddup party people!\n");

    system("ls -hal");

    puts("\n\nPuts is the same as printf except it's called withint the stdlib.h and it auto-adds a newline to the end of ever stament (just like echo does in bash and printf doesn't)");

    numchoose();

    return 0;
}

